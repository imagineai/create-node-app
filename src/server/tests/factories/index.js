import { buildComment, createComment } from './comment.factory';
import { buildPerson, createPerson } from './person.factory';
import { buildTodo, createTodo } from './todo.factory';

export {
  buildTodo,
  createTodo,
  buildComment,
  createComment,
  buildPerson,
  createPerson,
};
